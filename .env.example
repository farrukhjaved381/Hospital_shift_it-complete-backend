# Database Configuration
DATABASE_URL="postgresql://neondb_owner:npg_iVy4EdBSDCQ6@ep-empty-unit-ady1oggc-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# JWT Configuration
JWT_ACCESS_SECRET="your-super-secret-jwt-access-key-change-this-in-production"
JWT_REFRESH_SECRET="your-super-secret-jwt-refresh-key-change-this-in-production"
JWT_ACCESS_EXPIRATION="15m"
JWT_REFRESH_EXPIRATION="7d"

# Application Configuration
NODE_ENV="development"
PORT=3000

# Optional: Redis Configuration (for caching and sessions)
REDIS_URL="redis://localhost:6379"

# Optional: Email Configuration (for notifications)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"

# Optional: File Upload Configuration
MAX_FILE_SIZE=5242880  # 5MB
UPLOAD_DEST="./uploads"

# Optional: Rate Limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=10

# Optional: CORS Configuration
CORS_ORIGIN="http://localhost:3000,http://localhost:3001"
